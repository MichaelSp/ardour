#pragma once

// Ardour only includes this for old Boost.Bind compatibility.
// Newer Boost releases no longer ship the header, but the code path
// in this tree does not depend on any declarations from it.
